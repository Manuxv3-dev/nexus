/**
 * @deprecated remplacé par `EventsDashboard` + `EventModal` (cf. J5b #38).
 * Conservé temporairement comme placeholder vide jusqu'à suppression du
 * dossier `killer-features/` (J5b #45).
 */
export {};
